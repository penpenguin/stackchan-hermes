# Device gateway boundary

Authenticated WebSocket sessions, handshake/heartbeat, device registry, commands, binary routing,
capture upload, reconnect cleanup, and per-device resource limits belong here.
